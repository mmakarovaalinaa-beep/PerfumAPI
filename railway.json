{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn api.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
```
4. Click **Commit changes**

---

Also check your **`requirements.txt`** file in the repo:

1. Click on `requirements.txt`
2. Make sure it includes at least these lines — if anything is missing, add it and commit:
```
fastapi
uvicorn
supabase
python-dotenv
requests
beautifulsoup4
